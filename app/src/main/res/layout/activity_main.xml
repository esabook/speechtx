<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto"
	xmlns:tools="http://schemas.android.com/tools">

	<android.support.constraint.ConstraintLayout
		android:layout_width="match_parent"
		android:layout_height="match_parent">

		<ImageButton
			android:id="@+id/btn_record"
			android:layout_width="90dp"
			android:layout_height="90dp"
			android:layout_marginEnd="8dp"
			android:layout_marginRight="8dp"
			android:minWidth="40dp"
			android:minHeight="40dp"
			android:src="@android:drawable/ic_media_play"
			app:layout_constraintEnd_toEndOf="parent"
			app:layout_constraintStart_toStartOf="parent" />

		<TextView
			android:id="@+id/tv_query"
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			android:gravity="center"
			android:padding="30dp"
			app:layout_constraintTop_toBottomOf="@+id/btn_record"
			tools:text="hsjah sajhsjahs sa jahsjah jshajhsja shajshaj jshajshaj hsajhsaj shajshajsh jahjshajsh hjashajhs jahajshajsha" />

		<ListView
			android:id="@+id/list_dictionary"
			android:layout_width="match_parent"
			android:layout_height="0dp"
			android:paddingLeft="30dp"
			android:paddingRight="30dp"
			android:scrollbarStyle="outsideOverlay"
			app:layout_constraintBottom_toBottomOf="parent"
			app:layout_constraintTop_toBottomOf="@+id/tv_query"
			tools:listitem="@layout/item_dictionary_counter" />

	</android.support.constraint.ConstraintLayout>
</layout>